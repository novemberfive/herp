Nothing should be ignored since the added files are markdown documents describing business processes and state machines, which are source documentation files that should be tracked in version control.