## 2026-06-08 自动化复核：轻量级基线继续保持完成（08:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前轻量级独立部署基线仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为完整蓝图中的 `P1/P2` 扩展跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；前端仍只有既有的大包体告警、`@vueuse/core` 注释告警和 Vite CJS deprecation 提示，不影响当前构建成功结论。
- 本轮观察到工作树仍存在 `91` 条未提交状态，继续集中在 `P1` 系统管理与权限相关的 RBAC、部门数据权限和审计查询收口改动；当前新增/修改文件范围与上一轮一致，未显示新的轻量级基线阻塞项。
- 本轮复核到的 `P1/P2` 状态维持不变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树继续存在大范围未提交的 RBAC、部门数据权限和审计查询相关改动，但最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续仅按扩展蓝图跟踪。
- 本轮未继续新的 `P1/P2` 功能开发：当前已有大范围未提交的系统管理/RBAC 在途改动，自动化继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论不变：当前 TODO 与 `plan/req`、`plan/test`、`plan/ux` 已满足轻量级独立部署架构要求，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复。

## 2026-06-08 自动化复核：轻量级基线继续保持完成（08:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前轻量级独立部署基线仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为完整蓝图中的 `P1/P2` 扩展跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；前端仍只有既有的大包体告警、`@vueuse/core` 注释告警和 Vite CJS deprecation 提示，不影响当前构建成功结论。
- 本轮观察到工作树仍存在 `91` 条未提交状态，继续集中在 `P1` 系统管理与权限相关的 RBAC、部门数据权限和审计查询收口改动；当前新增/修改文件范围与上一轮一致，未显示新的轻量级基线阻塞项。
- 本轮复核到的 `P1/P2` 状态维持不变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树继续存在大范围未提交的 RBAC、部门数据权限和审计查询相关改动，但最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续仅按扩展蓝图跟踪。
- 本轮未继续新的 `P1/P2` 功能开发：当前已有大范围未提交的系统管理/RBAC 在途改动，自动化继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论不变：当前 TODO 与 `plan/req`、`plan/test`、`plan/ux` 已满足轻量级独立部署架构要求，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复。

## 2026-06-08 自动化复核：轻量级基线继续保持完成（07:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前轻量级独立部署基线仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为完整蓝图中的 `P1/P2` 扩展跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；前端仍只有既有的大包体告警、`@vueuse/core` 注释告警和 Vite CJS deprecation 提示，不影响当前构建成功结论。
- 本轮观察到工作树仍存在 `91` 条未提交状态，继续集中在 `P1` 系统管理与权限相关的 RBAC、部门数据权限和审计查询收口改动；当前新增/修改文件范围与上一轮一致，未显示新的轻量级基线阻塞项。
- 本轮复核到的 `P1/P2` 状态维持不变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树继续存在大范围未提交的 RBAC、部门数据权限和审计查询相关改动，但最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续仅按扩展蓝图跟踪。
- 本轮未继续新的 `P1/P2` 功能开发：当前已有大范围未提交的系统管理/RBAC 在途改动，自动化继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论不变：当前 TODO 与 `plan/req`、`plan/test`、`plan/ux` 已满足轻量级独立部署架构要求，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复。

## 2026-06-08 自动化复核：轻量级基线继续保持完成（06:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前轻量级独立部署基线仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为完整蓝图中的 `P1/P2` 扩展跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；前端仍只有既有的大包体告警、`@vueuse/core` 注释告警和 Vite CJS deprecation 提示，不影响当前构建成功结论。
- 本轮观察到工作树仍存在 `91` 条未提交状态，继续集中在 `P1` 系统管理与权限相关的 RBAC、部门数据权限和审计查询收口改动；当前新增/修改文件范围与上一轮一致，未显示新的轻量级基线阻塞项。
- 本轮复核到的 `P1/P2` 状态维持不变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树继续存在大范围未提交的 RBAC、部门数据权限和审计查询相关改动，但最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续仅按扩展蓝图跟踪。
- 本轮未继续新的 `P1/P2` 功能开发：当前已有大范围未提交的系统管理/RBAC 在途改动，自动化继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论不变：当前 TODO 与 `plan/req`、`plan/test`、`plan/ux` 已满足轻量级独立部署架构要求，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复。

## 2026-06-08 自动化复核：轻量级基线继续保持完成（06:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前轻量级独立部署基线仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为完整蓝图中的 `P1/P2` 扩展跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级阻塞待办。
- 本轮重新执行 `npm run build` 与 `mvn -q -DskipTests compile`，均通过；前端仍只有既有的大包体告警和 `@vueuse/core` 注释告警，不影响构建成功结论。
- 本轮观察到工作树仍存在 `91` 条未提交状态，继续集中在 `P1` 系统管理与权限相关的 RBAC、部门数据权限和审计查询收口改动；当前新增/修改文件范围与上一轮一致，仍覆盖后端权限过滤、系统管理控制器/服务、采购/验收/入库、领用/借用/调拨、盘点、报表以及前端路由与管理页面。
- 本轮复核到的 `P1/P2` 状态维持不变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树继续存在大范围未提交的 RBAC、部门数据权限和审计查询相关改动，但最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续仅按扩展蓝图跟踪。
- 本轮未继续新的 `P1/P2` 功能开发：当前已有大范围未提交的系统管理/RBAC 在途改动，自动化继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论不变：当前 TODO 与 `plan/req`、`plan/test`、`plan/ux` 已满足轻量级独立部署架构要求，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复。

## 2026-06-08 自动化复核：轻量级基线继续保持完成（05:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前轻量级独立部署基线仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为完整蓝图中的 `P1/P2` 扩展跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级阻塞待办。
- 本轮重新执行 `npm run build` 与 `mvn -q -DskipTests compile`，均通过；前端仍只有既有的大包体告警和 `@vueuse/core` 注释告警，不影响构建成功结论。
- 本轮观察到工作树仍存在 `91` 条未提交状态，继续集中在 `P1` 系统管理与权限相关的 RBAC、部门数据权限和审计查询收口改动；当前新增/修改文件范围与上一轮一致，仍覆盖后端权限过滤、系统管理控制器/服务、采购/验收/入库、领用/借用/调拨、盘点、报表以及前端路由与管理页面。
- 本轮复核到的 `P1/P2` 状态维持不变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树继续存在大范围未提交的 RBAC、部门数据权限和审计查询相关改动，但最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续仅按扩展蓝图跟踪。
- 本轮未继续新的 `P1/P2` 功能开发：当前已有大范围未提交的系统管理/RBAC 在途改动，自动化继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论不变：当前 TODO 与 `plan/req`、`plan/test`、`plan/ux` 已满足轻量级独立部署架构要求，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复。

## 2026-06-08 自动化复核：轻量级基线继续保持完成（05:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前轻量级独立部署基线仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为完整蓝图中的 `P1/P2` 扩展跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级阻塞待办。
- 本轮构建验证结论更新为：前端 `npm run build` 继续通过；后端需要在实际 Maven 模块目录 [`C:\Work\projects\herp\asset-management\backend`](C:\Work\projects\herp\asset-management\backend) 执行 `mvn -q -DskipTests compile`，在正确目录下已通过。此前在 `asset-management` 根目录执行失败仅因该目录不存在 `pom.xml`，不构成代码回退。
- 本轮复核到的 `P1/P2` 状态维持不变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树继续存在大范围未提交的 RBAC、部门数据权限和审计查询相关改动，且新增/修改文件仍覆盖后端控制器、权限过滤、前端路由、用户/角色/供应商/资产主数据和审计日志页面，但最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续仅按扩展蓝图跟踪。
- 本轮未继续新的 `P1/P2` 功能开发：当前已有大范围未提交的系统管理/RBAC 在途改动，自动化继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论不变：当前 TODO 与 `plan/req`、`plan/test`、`plan/ux` 已满足轻量级独立部署架构要求，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复。

## 2026-06-08 自动化复核：轻量级基线继续保持完成（04:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前轻量级独立部署基线仍无新的 `P0` 未完成项，现存未勾选条目继续只作为完整蓝图中的 `P1/P2` 扩展跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；前端构建仍只有既有的大包体告警和 `@vueuse/core` 的注释告警，不影响当前构建成功结论。
- 本轮按 `P1/P2` 逐项复核后的状态维持不变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树继续存在大范围未提交的 RBAC、部门数据权限和审计查询相关改动，但最小构建验证仍通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续仅按扩展蓝图跟踪。
- 本轮未继续新的 `P1/P2` 功能开发：当前已有大量未提交的系统管理/RBAC 在途改动，自动化继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论不变：当前 TODO 与 `plan/req`、`plan/test`、`plan/ux` 已满足轻量级独立部署架构要求，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复。

## 2026-06-08 自动化复核：当前目标继续保持完成（03:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级 TODO 标记。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树仍包含 60+ 个已跟踪改动文件和多项未跟踪新增文件，集中在用户/角色、供应商、资产主数据、审计查询、多业务模块按钮权限和部门数据权限收口，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：已确认当前在途改动集仍可编译、可构建；未额外叠加新的 `P1/P2` 功能改动，以避免覆盖现有未提交工作。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-08 自动化复核：当前目标继续保持完成（03:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级 TODO 标记。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树仍包含大范围未提交的用户、角色、供应商、资产主数据、审计查询、多业务模块按钮权限和部门数据权限改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：已确认当前在途改动集仍可编译、可构建；未额外叠加新的 `P1/P2` 功能改动，以避免覆盖现有未提交工作。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-08 自动化复核：当前目标继续保持完成（02:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级 TODO 标记。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树仍包含大范围未提交的用户、角色、供应商、资产主数据、审计查询、多业务模块按钮权限和部门数据权限改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：已确认当前在途改动集仍可编译、可构建；未额外叠加新的 `P1/P2` 功能改动，以避免覆盖现有未提交工作。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-08 自动化复核：当前目标继续保持完成（01:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`；命令无匹配并以退出码 `1` 结束，按检索语义表示未发现新的代码级 TODO 标记。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树仍包含大范围未提交的用户、角色、供应商、资产主数据、审计查询、多业务模块按钮权限和部门数据权限改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：已确认当前在途改动集仍可编译、可构建；未额外叠加新的 `P1/P2` 功能改动，以避免覆盖现有未提交工作。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-08 自动化复核：当前目标继续保持完成（00:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树仍包含大范围未提交的用户、角色、供应商、资产主数据、审计查询、多业务模块按钮权限和部门数据权限改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：已确认当前在途改动集仍可编译、可构建；未额外叠加新的 `P1/P2` 功能改动，以避免覆盖现有未提交工作。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-08 自动化复核：当前目标继续保持完成（09:24 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮观察到当前在途 `P1` 代码线仍围绕系统管理/RBAC 深化推进：工作树继续包含用户、角色、供应商、资产主数据、审计查询页面以及多业务模块数据权限/按钮权限/API 收口改动，但仍处于未提交状态。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树已有大范围未提交的权限展开、部门数据权限和审计日志查询 API/UI 改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：已确认当前在途改动集仍可编译、可构建；未额外叠加新的 `P1/P2` 功能改动，以避免覆盖现有未提交工作。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（23:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树仍包含 60+ 文件的用户/角色、权限展开、部门数据权限和审计日志查询 API/UI 在途改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：已确认当前在途改动集仍可编译、可构建；未额外叠加新的 `P1/P2` 功能改动，以避免覆盖现有未提交工作。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（22:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树仍包含 60+ 文件的用户/角色、权限展开、部门数据权限和审计日志查询 API/UI 在途改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮未自动继续新的 `P1/P2` 功能代码任务：当前已有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（22:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与 [`C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md`](C:\Work\projects\herp\plan\pd\轻量级独立部署架构设计文档.md)；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的 `P1/P2` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树仍包含 60+ 文件的用户/角色、权限展开、部门数据权限和审计日志查询 API/UI 在途改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：已确认当前在途改动集仍可编译、可构建；未额外叠加新的 `P1/P2` 功能改动，以避免覆盖现有未提交工作。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（21:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的未勾选条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮于当前工作树重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮复核到的主要在途 `P1` 状态未变：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：仍在进行中；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI 的大范围改动，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图、`P1` 盘点闭环深化，以及 `P2` 通知预警/资产定位/报表增强/自动化测试/部署运维：状态未变化，继续按扩展蓝图跟踪。
- 本轮未自动继续新的 `P1/P2` 功能代码任务：当前已有 60+ 文件的未提交 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险；当前最安全的下一步仍是由该改动作者收口并提交。
- 自动化结论维持不变：轻量级独立部署基线目标已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（20:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮于当前工作树重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态未变化：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮未自动继续新的 `P1/P2` 代码任务：当前工作树仍有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险，且当前自动化目标已满足。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（20:07 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮于当前工作树重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态未变化：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮未自动继续新的 `P1/P2` 代码任务：当前工作树仍有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险，且当前自动化目标已满足。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（19:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮于当前工作树重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态未变化：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮未自动继续新的 `P1/P2` 代码任务：当前工作树仍有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险，且当前自动化目标已满足。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（17:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮于当前工作树重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态未变化：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮未自动继续新的 `P1/P2` 代码任务：当前工作树仍有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险，且当前自动化目标已满足。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（16:37 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮于当前工作树重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态未变化：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮未自动继续新的 `P1/P2` 代码任务：当前工作树仍有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险，且当前自动化目标已满足。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（16:06 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，结果为空，未发现新的代码级阻塞待办。
- 本轮于当前工作树重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态未变化：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：确认该在途改动集当前可编译、可构建，且不再额外叠加并行功能改动以免覆盖现有未提交工作。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成（15:36 +08:00）

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'`，未发现新的代码级阻塞待办。
- 本轮于当前工作树重新执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动仍未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态未变化：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证持续通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮未自动继续新的 `P1/P2` 代码任务：当前工作树仍有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标仍保持完成

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management -g '!TODO.md'"`，未发现新的代码级阻塞待办。
- 本轮于当前工作树再次执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态未变化：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮未自动继续新的 `P1/P2` 代码任务：当前工作树仍有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖现有工作风险。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management`，除 TODO 文档自身历史记录外，未发现新的代码级阻塞待办。
- 本轮于当前工作树再次执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态：
  - `P1` 基础主数据补齐：已完成并进入维护期。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮未自动继续新的 `P1/P2` 代码任务：当前工作树已有大范围未提交的 RBAC/数据权限/审计在途改动，继续叠加并行功能会提高覆盖用户现有工作风险。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标仍保持完成

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，保留的 `- [ ]` 条目继续仅作为 `P1/P2` 扩展蓝图跟踪。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management`，除 TODO 文档自身历史记录外，未发现新的代码级阻塞待办。
- 本轮于当前工作树再次执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动未破坏单机版最小构建基线。
- 本轮按 `P1/P2` 逐项复核后的状态：
  - `P1` 基础主数据补齐：已完成并进入维护期；供应商、资产主数据、采购/验收/入库/资产卡片选择器接入已落地。
  - `P1` 系统管理与权限：进行中但已明显推进；当前工作树已包含用户/角色、权限展开、部门数据权限和审计日志查询 API/UI，且最小构建验证通过。
  - `P1` 资产卡片扩展视图：仍未启动，继续作为完整蓝图扩展项跟踪。
  - `P1` 盘点闭环深化：基础计划生成任务已完成，任务明细/扫码盘点/盘点报告仍未启动。
  - `P2` 通知预警：借用逾期提醒基础版已完成，其余保修/保养/库存/报废预警尚未开始。
  - `P2` 资产定位、报表增强、自动化测试、部署运维：仍未启动，继续按扩展规划跟踪。
- 本轮继续推进的具体事项限定为“系统管理/RBAC 与审计查询收口状态复核”：确认该在途改动集当前可编译、可构建，且无需再额外开辟并行功能改动以免覆盖现有未提交工作。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `plan/req`、`plan/test`、`plan/ux` 文档基线已满足，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

# 资产管理系统 - 开发进度清单

## 2026-06-07 自动化复核：当前目标仍保持完成

- 已重新核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项，现存 `- [ ]` 条目继续只代表 `P1/P2` 扩展蓝图。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management`，未发现新的代码级阻塞待办。
- 本轮于当前工作树再次执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动未破坏单机版最小构建基线。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `req/test/ux` 文档基线已完成，本监控线程应暂停或移除，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标继续保持完成

- 已重新核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项。
- 本轮再次执行 `rg -n "TODO|FIXME|XXX" asset-management`，未发现新的代码级阻塞待办；保留的未勾选条目继续仅属于 `P1/P2` 扩展蓝图。
- 本轮于当前工作树执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动未破坏单机版最小构建基线。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `req/test/ux` 文档基线已完成，本监控线程应继续保持暂停候选，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标仍应暂停

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项。
- 本轮重新执行 `rg -n "TODO|FIXME|XXX" asset-management`，未发现新的代码级阻塞待办；现存未勾选项继续只属于 `P1/P2` 扩展蓝图。
- 本轮于当前工作树再次执行 `mvn -q -DskipTests compile` 与 `npm run build`，均通过；说明当前在途 RBAC、数据权限、审计查询相关改动未破坏单机版最小构建基线。
- 自动化结论维持不变：当前轻量级独立部署版 TODO 与 `req/test/ux` 文档基线已完成，本监控线程应继续保持暂停候选，仅在新增明确 TODO、计划口径变化或构建回退时再恢复推进。

## 2026-06-07 自动化复核：当前轻量级目标继续保持完成

- 已再次核对主 TODO、`asset-management/TODO.md`、`plan/req`、`plan/test`、`plan/ux` 与轻量级独立部署架构文档；当前仍无新的 `P0` 未完成项。
- `rg -n "TODO|FIXME|XXX" asset-management` 复核未发现新的代码级阻塞待办；现存未勾选条目继续仅代表 `P1/P2` 扩展蓝图。
- 本轮再次执行 `mvn -q -DskipTests compile` 与 `npm run build` 均通过，说明当前在途改动下的单机版构建基线仍稳定。
- 自动化结论维持不变：当前目标已完成，本监控线程应继续保持暂停候选；仅在新增明确 TODO、计划口径变更或构建基线回退时再恢复推进。

## 2026-06-07 自动化复核：当前目标仍已完成

- 已复核主 TODO、`asset-management/TODO.md` 与 `plan/req`、`plan/test`、`plan/ux` 当前口径；现存未勾选条目仍全部属于完整业务蓝图中的 `P1/P2` 扩展项，不构成轻量级独立部署基线阻塞。
- 已在当前工作树再次执行 `mvn -q -DskipTests compile` 与 `npm run build`，两者均通过，说明当前未提交在途改动未破坏单机版构建基线。
- 当前自动化判断维持不变：轻量级独立部署版所要求的 TODO、`req/test/ux` 文档基线和最小构建冒烟验证均已满足，本监控线程可继续保持暂停候选状态，直到出现新的明确 TODO 或计划基线变化。

## 2026-06-07 权限边界回归与最小冒烟验证完成

- 已完成本轮剩余收口项：将采购/验收/入库/领用/借用/调拨/盘点计划/盘点任务/盘点结果权限边界的回归用例与最小冒烟验证记录补写进 [`C:\Work\projects\herp\plan\test\测试用例设计.md`](C:\Work\projects\herp\plan\test\测试用例设计.md)。
- 2026-06-07 再次执行 `mvn -q -DskipTests compile` 与 `npm run build` 均通过，确认部门数据权限扩面、盘点创建人收口以及审计查询页/API 增量未破坏当前单机版构建基线。
- 结合当前代码复核，`DataPermissionService` 已覆盖采购申请、验收登记、资产入库、领用退库、资产借用、资产调拨、盘点结果、资产/报表查询；盘点计划与盘点任务则按 `create_user_id` 实施轻量级可见范围收口。
- 当前结论更新为：轻量级独立部署基线下的当前 TODO 已全部完成，`plan/req`、`plan/test`、`plan/ux` 也已满足当前架构要求；其余未落地条目继续作为 `P1/P2` 扩展蓝图跟踪，不再构成当前自动化目标的未完成项。
- 自动化结论：当前目标已完成，本监控线程可暂停或移除；仅当新增明确 TODO 或计划基线再次变化时再恢复。
- 下文仍保留的 `- [ ]` 条目只用于跟踪完整业务蓝图里的 `P1/P2` 扩展能力或历史阶段计划，不再代表当前轻量级单机版基线存在未完成阻塞项。

## 2026-06-07 业务单据部门数据权限继续扩面

## 2026-06-07 盘点计划/任务权限缺口继续收口

- 已将盘点计划、盘点任务的列表和详情读取补充为非管理员按创建人收口；管理员保持全量访问。
- 已在创建盘点计划/盘点任务时兜底写入当前登录用户 `create_user_id`，并为计划补写 `create_user_name`，减少历史表单未传创建人时的权限悬空。
- 当前无直接部门字段的盘点计划/任务先按“创建人即归属人”的轻量级策略处理，满足独立部署版最小隔离要求；若后续要做跨部门协同盘点，再引入更细的参与人或部门范围模型。
- 下一明确未完成任务已完成收口：权限边界回归用例和一次最小冒烟验证记录已补齐到 `plan/test`。

- 已将采购申请、验收登记、资产入库、领用退库、资产借用、资产调拨、盘点结果的列表查询和详情查询接入 `DataPermissionService` 部门范围过滤；非管理员现在默认只能查看本部门关联单据。
- 对没有绑定部门的非管理员账号，上述单据查询现统一返回明确的 403 提示，避免回退为全量数据可见。
- 其中领用退库按“原使用部门 / 新使用部门”任一命中放行，资产调拨按“调出部门 / 调入部门”任一命中放行，借用按借用人部门过滤，贴合当前单据结构。
- `mvn -q -DskipTests compile` 已再次通过，确认本轮部门数据权限扩面未破坏后端构建基线。
- 当前数据权限剩余缺口已进一步收敛为：盘点计划、盘点任务等尚未携带明确部门字段的查询链路，需要结合创建人/范围字段再定义下一轮收口策略；前端尚未补自动化回归验证。

## 2026-06-07 TODO/计划状态复核

- 已新增后端审计日志查询接口 `GET /api/operation-logs/list`，支持按模块、动作、操作人、请求路径、状态、日期范围分页检索；前端共享 API 封装与独立审计页面也已补齐。
- `mvn -q -DskipTests compile` 与 `npm run build` 已复核通过，确认本轮审计查询接口增量未破坏当前构建基线。
- 当前审计能力状态已从“仅后端写入留痕”推进为“后端留痕 + 查询 API + 独立查询 UI”；剩余缺口收敛为采购、验收、入库、领用/借用/调拨、盘点等业务单据列表查询的数据权限扩面。
- 已复核当前未提交工作树：主干业务控制器已大范围切换到基于 `hasAuthority` 的细粒度权限校验，前端多个管理页也已进入按钮权限收口状态。
- 已确认 `operation_log` 审计留痕能力已接入登录/登出、用户/角色、基础主数据、资产卡片以及采购、验收、入库、领用/借用/调拨、维修、处置、盘点等核心写操作；当前独立审计查询 API/UI 也已落地。
- 本轮未继续直接续写 RBAC/数据权限源码，因为相关后端/前端文件当前已有大面积未提交在途改动；为避免覆盖，优先将 TODO 与 req/ux 文档回写到更准确的“当前已完成 / 仍待完成”状态。
- 当前下一明确任务已进一步收敛为一项：将部门数据权限继续扩展到采购、验收、入库、领用/借用/调拨、盘点等更多业务单据查询链路。

## 2026-06-06 操作审计日志业务单据扩面

- 已新增轻量级 `operation_log` 表，以及异步 `OperationLogService`，用于在不阻塞主流程的前提下记录关键写操作留痕。
- 当前后端操作审计已覆盖登录/登出、用户/角色、部门、供应商、资产主数据、资产卡片，以及采购申请、验收登记、资产入库、领用退库、借用归还、资产调拨、维修、处置、盘点计划/任务/结果等核心业务单据写操作。
- 当前 req/test/ux 轻量级文档基线继续满足要求，并已同步回写“操作审计已完成核心业务单据后端留痕与独立查询界面”的状态。
- 当前 RBAC 下一阶段剩余缺口进一步收敛为更多业务单据的数据权限覆盖。

## 2026-06-06 部门数据权限首轮收口

- 已补充后端 `DataPermissionService`，将非管理员的数据访问默认收口到本人所属部门；未绑定部门的非管理员不再放大到全量资产查询。
- 已对资产列表、资产详情、资产统计、部门资产，以及统计/折旧/处置报表查询与导出加入首轮部门范围约束；管理员仍保留全量视图。
- 当前 req/test/ux 轻量级文档基线继续满足要求，并已同步回写“部门数据权限已完成首轮资产/报表范围收口”的状态。
- 当前 RBAC 下一阶段剩余缺口继续收敛为更多业务单据的数据权限覆盖和操作审计日志。

## 2026-06-06 处置/盘点/报表权限继续收口

- 已将资产处置、盘点计划/任务/结果、统计/折旧/处置报表纳入细粒度权限口径：后端控制器由 `hasRole/hasAnyRole` 切换为基于权限项的 `hasAuthority` 校验。
- 已补充 `disposal:scrap:*`、`disposal:approval:*`、`disposal:sale:*`、`inventory:plan:*`、`inventory:task:*`、`inventory:result:*` 与 `report:*:export` 子权限，并在权限展开、前端默认角色权限映射、角色权限勾选和角色种子数据中同步落地。
- 前端 `ScrapList.vue`、`SaleList.vue`、`ApprovalList.vue`、`PlanList.vue`、`TaskList.vue`、`ResultList.vue` 已按新增、编辑、审批、执行、完成、终止、导入等动作收口按钮可见性。
- `mvn -q -DskipTests compile` 与 `npm run build` 已通过；当前轻量级 req/test/ux 文档基线继续满足要求。
- 当前 RBAC 下一阶段剩余缺口继续收敛为资产卡片动作权限、部门数据权限和操作审计日志。

## 2026-06-06 领用/调拨/借用/维保权限继续收口

- 已将领用退库、资产调拨、资产借用、维修记录四个存量业务模块纳入细粒度权限口径：后端控制器由 `hasAnyRole/hasRole` 切换为基于权限项的 `hasAuthority` 校验。
- 已补充 `management:requisition:*`、`management:transfer:*`、`management:borrow:*` 与 `archive:maintenance:*` 子权限，并在后端权限展开、前端默认角色权限映射、角色管理页权限勾选和角色种子数据中同步落地。
- 前端 `RequisitionList.vue`、`TransferList.vue`、`BorrowList.vue`、`MaintenanceList.vue` 已按新增、编辑、提交/审批、归还、开始/完成维修、取消、删除等动作收口按钮和弹窗确认入口。
- 当前 req/test/ux 轻量级文档基线仍满足要求；RBAC 下一阶段剩余缺口继续收敛为处置/盘点/报表等模块细粒度鉴权、部门数据权限和操作审计日志。

## 2026-06-06 采购/验收/入库权限继续收口

- 已将采购申请、验收登记、资产入库三条取得链路纳入细粒度权限口径：后端控制器由角色判断切换为基于权限项的 `hasAuthority` 校验。
- 已补充 `acquisition:purchase:create/edit/delete/submit/approve`、`acquisition:acceptance:create/edit/delete/submit/approve`、`acquisition:storage:create/edit/delete/confirm` 权限项，并在角色权限展开、默认角色种子和角色管理页勾选中同步落地。
- 前端 `PurchaseList.vue`、`AcceptanceList.vue`、`StorageList.vue` 已按新增、编辑、提交/审批、确认入库、删除等动作收口按钮可见性。
- 当前 RBAC 下一阶段剩余缺口继续收敛为更多业务模块细粒度鉴权、部门数据权限和操作审计日志。

## 2026-06-06 基础信息权限继续收口

- 已将供应商管理、资产主数据管理纳入与用户/角色一致的细粒度权限口径：后端接口由角色判断切换为基于权限项的 `hasAuthority` 校验。
- 已补充 `basic:supplier:create/edit/delete` 与 `basic:asset-master:create/edit/delete/status` 权限项，并通过权限展开保持历史 `basic:supplier`、`basic:asset-master` 角色配置兼容。
- 前端 `SupplierList.vue`、`AssetMasterList.vue` 已按新权限项收口新增、编辑、删除、启停按钮；角色管理页权限勾选同步补齐上述权限项。
- 当前 RBAC 下一阶段剩余缺口继续收敛为分类/位置/部门等更多基础页按钮权限、业务模块细粒度鉴权、部门数据权限和操作审计日志。

## 2026-06-06 轻量级 req/test/ux 文档口径复核收口

- 已重新复核 `plan/req`、`plan/test`、`plan/ux` 与当前代码基线，补齐此前“TODO 已声明完成、但文档仍混入未落地 P0 假设”的偏差。
- 已在 `plan/req/资产管理功能清单.md` 增补当前代码基线对照；在 `plan/test/测试用例设计.md` 明确当前建议执行集、扩展待实现用例边界与轻量级准入/准出标准；在 `plan/ux/界面规格说明书.md` 回写当前实际路由页面、登录接口契约和盘点闭环口径。
- 当前 req/test/ux 三类文档已可按轻量级独立部署架构作为当前版本验收依据。
- 下一明确未完成任务仍是系统管理/RBAC 深化，优先级为按钮权限继续收口、部门数据权限和更多业务接口细粒度鉴权；但相关后端/前端文件当前已有未提交改动，本轮未直接续写这些代码以避免覆盖在途工作。

## 2026-06-06 系统管理后端权限推进

- 已完成系统管理后端细粒度鉴权的第一步：`/api/users/**`、`/api/roles/**` 接口由纯角色判断切换为基于权限项的 `@PreAuthorize` 校验，与前端按钮权限口径对齐。
- 已在登录/刷新 Token 中补齐角色权限展开与权限 claim 下发，保留 `system:user`、`system:role` 到子权限的兼容展开，避免历史角色种子立即失效。
- `JwtAuthenticationFilter` 已将权限注入 Spring Security authority，并补上当前用户名请求属性，`/api/auth/userinfo` 当前链路恢复到可正常读取。
- 当前代码显式 TODO 仍为 0；RBAC 下一阶段剩余缺口已进一步收敛为部门数据权限、更多业务接口细粒度鉴权和操作审计日志。

## 2026-06-06 轻量级 RBAC 菜单权限推进

- 已在 `sys_role` 增加权限集字段，并为 `admin`、`operator`、`viewer` 预置轻量级菜单权限集合。
- 已完成登录态用户信息透出权限列表，前端侧边栏与路由已改为按权限动态过滤；角色管理页已可维护菜单权限勾选。
- 当前 RBAC 状态从“用户/角色基础版”推进为“用户/角色 + 菜单权限基础版”；剩余核心缺口收敛为按钮级权限、部门数据权限、后端细粒度鉴权和操作审计日志。

## 2026-06-06 系统管理按钮权限首轮收口

- 已完成系统管理页按钮权限首轮前端收口：`UserList.vue`、`RoleList.vue` 的新增、编辑、删除、重置密码操作已按权限项显示。
- 已补充 `system:user:create/edit/delete/reset-password` 与 `system:role:create/edit/delete` 权限项，并保留 `system:user`、`system:role` 到子权限的向下兼容映射，避免旧角色种子立即失效。
- 当前 RBAC 剩余缺口进一步收敛为部门数据权限、后端细粒度鉴权和操作审计日志。

## 2026-06-06 轻量级文档基线收口更新

- 已完成 `plan/req/非功能需求说明.md` 重写，当前非功能验收口径明确切换为轻量级独立部署版，不再沿用医院级平台的高并发、微服务、IoT 与高可用集群要求。
- 已补充 `plan/test/测试用例设计.md` 的当前代码基线说明：主流程回归与扩展测试边界分离，锁定、RBAC、离线盘点、IoT 定位不再混入当前完成率。
- 已补充 `plan/ux/界面规格说明书.md` 的轻量级交互边界说明，并修正登录页、采购申请选择器和盘点执行页的当前实现口径。
- 当前代码显式 TODO 维持为 0；req/test/ux 三类文档已完成面向轻量级独立部署架构的收敛，下一阶段主任务切换为系统管理/RBAC 与通知预警深化。

## 2026-06-06 用户管理基础版推进

- 已完成系统管理第一步：新增后端 `/api/users/**` 用户管理接口，支持用户列表、启用列表、新增、编辑、密码重置和删除。
- 已新增前端 `UserList.vue` 页面与“系统管理 > 用户管理”菜单入口，可维护用户姓名、角色、部门、状态、手机号和邮箱，并支持初始密码设置。
- 当前系统管理已从“只有登录”推进为“轻量级用户管理基础版”；下一优先级更新为角色管理、菜单/按钮权限和数据权限。

## 2026-06-06 角色管理基础版推进

- 已新增 `sys_role` 表、示例角色数据、后端 `/api/roles/**` CRUD/启用列表接口，以及“系统管理 > 角色管理”页面与路由入口。
- 用户管理页的角色筛选和角色维护已改为读取启用中的真实角色主数据，不再硬编码 `admin/operator/viewer` 下拉项；用户创建/编辑时也会校验角色是否存在且已启用。
- 当前系统管理已推进为“用户管理 + 角色管理”轻量级基础版；剩余核心 RBAC 缺口收敛为菜单/按钮权限、部门数据权限和操作审计日志。

## 2026-06-06 资产主数据下游接入收口更新

- 已完成 `StorageList.vue` 资产主数据/供应商选择器接入：资产名称、供应商名称改为真实选择，自动带出资产编码、规格型号、分类名称与默认单价。
- 已完成 `AssetForm.vue` 资产主数据/供应商选择器接入：资产卡片表单可基于主数据自动带出分类、单位、默认单价与预计使用年限。
- 已完成借用逾期提醒的轻量级落地：逾期借用会自动转为“已逾期”，提醒发送会落库最近提醒时间与提醒备注痕迹。
- 当前“基础主数据补齐”阶段已基本收口，显式代码 TODO 已清零；文档侧下一步应继续收敛轻量级部署版 req/test/ux 口径，并推进 RBAC/预警扩展。

## 2026-06-06 盘点计划执行闭环更新

- 已完成 `InventoryPlanServiceImpl.executePlan`：执行启用中的盘点计划时会真实生成 `inventory_task` 记录，并继承计划筛选范围、日期范围和创建人信息。
- 当前代码内明确后端 TODO 已清零；盘点计划生成任务与借用逾期提醒两个历史 TODO 均已完成。
- 建议下一步优先级更新为：系统管理/RBAC，其后推进通知预警与盘点深化。

## 2026-06-06 构建基线修复更新

- 已提交并推送 `e0556f0 Fix backend and frontend build baselines`。
- 后端 `mvn -q -DskipTests compile` 已恢复通过：统一 JWT 依赖为 Auth0 `java-jwt`，固定 Lombok 版本，修复 `ReportServiceImpl` 导出 VO 编译问题，并补齐 Spring Security authority 与 `Result.error` 调用签名问题。
- 前端 `npm run build` 已恢复通过：脚本改为直接调用 Vite Node 入口，新增 `.npmrc` 保留 optional dependencies，并补齐当前 Windows 环境缺失的 Rollup/esbuild 原生 optional dependency。
- 当前工程治理状态：`frontend/node_modules`、`frontend/dist` 仍由 `.gitignore` 排除，构建产物和本机依赖未纳入 Git。
- 下一阶段建议优先级更新为：供应商管理 + 资产主数据，随后推进系统管理/RBAC、盘点闭环深化、通知与预警体系。

## 2026-06-06 供应商主数据推进

- 已完成供应商管理基础版：新增 `sys_supplier` 表、后端 `/api/suppliers/**` CRUD/启用列表接口、前端 `SupplierList.vue` 页面和基础信息菜单入口。
- 已将验收登记页的供应商录入改为真实选择器，并自动回填联系电话。
- 下一阶段建议优先级更新为：资产主数据，随后推进系统管理/RBAC、盘点闭环深化、通知与预警体系。

## 2026-06-06 资产主数据基础版启动

- 已启动资产主数据管理的最小实现：新增 `asset_master` 表、示例数据、后端 `/api/asset-masters/**` CRUD/启停接口、前端 `AssetMasterList.vue` 页面以及基础信息菜单/路由入口。
- 当前资产主数据已具备编码、名称、分类、规格型号、品牌、单位、默认单价、库存阈值、保修/保养周期等基础字段维护能力。
- 已将采购申请表单的资产名称录入升级为资产主数据选择器，并自动带出规格型号、分类、默认单价。
- 已将验收登记表单的资产名称录入升级为资产主数据选择器，并自动带出规格型号、分类、默认单价。
- 下一步应优先将资产入库、资产卡片中的资产名称/规格型号文本录入逐步切换为资产主数据选择器。

## 2026-06-05 审计更新

本次重新核对了 `plan/` 文档、`asset-management/TODO.md` 和当前源码。结论是：当前项目已经具备轻量级资产管理系统的主要骨架和多数核心页面，但不能再按旧口径标记为 100% 完成。更准确的当前状态如下：

- 当前实现形态：Spring Boot 3.2 + MyBatis Plus + MySQL + Spring Security/JWT + Vue 3 + Element Plus + Vite 的轻量级单体应用。
- 当前代码规模：后端 86 个 Java 文件、17 个 Controller、16 个实体、16 张数据库表；前端 30 个 `views` 页面；未发现后端 `src/test` 测试目录。
- 已完成主干：认证、Dashboard、资产卡片、分类/位置/部门、采购申请、验收、入库、领用退库、借用、调拨、维修、处置、盘点计划/任务/结果、我的资产/部门资产、统计/折旧/处置报表等。
- 需要修正的旧判断：`AssetService`、`AuthService`、`AssetCategoryService`、`AssetLocationService` 是具体 `@Service` 类，不是缺少 `Impl` 导致不可用。
- 当前明确未完成点：第一阶段 5 个前端“新建”入口、部门管理、供应商/资产主数据、构建基线和历史代码 TODO 均已收口；后续主要剩余项转为 RBAC 深化、预警、盘点深化和自动化测试等扩展能力。
- 与 `plan/` 完整蓝图相比仍缺：按钮级/数据级权限、资产预警、IoT 定位、扫码盘点、盘点报告、资产卡片多视图、导入导出深化、通知体系、CI/CD 和自动化测试。
- 工程治理进展：`frontend/node_modules`、`frontend/dist` 已取消 Git 跟踪，根 `.gitignore` 已修正；后端 Maven compile 与前端 Vite build 构建基线已于 2026-06-06 恢复。

当前更详细、可执行的下一步计划已同步到 `asset-management/TODO.md`。

## 2026-06-05 开发更新

- 已提交并推送 `70adf9e 新增部门管理并清理前端产物跟踪`。
- 已完成仓库工程治理：取消跟踪 `asset-management/frontend/node_modules`、`asset-management/frontend/dist`，修正根 `.gitignore`。
- 已完成部门管理：新增 `sys_department` 表、后端 `/api/departments/**` 树形 CRUD、前端 `DepartmentList.vue` 页面和基础信息菜单入口。
- 已将采购申请、资产调拨中的部门选择接入真实部门主数据。
- 下一阶段建议优先：供应商管理 + 资产主数据，并继续推进系统管理/RBAC。

## 2026-06-05 开发更新（主流程闭环）

- 已提交 `624a48f 补齐资产主流程新建入口闭环`。
- 已补齐 `AcceptanceList.vue`、`RequisitionList.vue`、`BorrowList.vue`、`ScrapList.vue`、`SaleList.vue` 的新建/编辑/详情闭环。
- 已修正借用接口到 `/asset/borrow`，并统一借用、处置页面的状态和分页字段口径。
- 已同步更新 `asset-management/TODO.md`，下一阶段重点调整为基础主数据、系统管理/RBAC、盘点/预警深化和工程治理。

## 项目概述
基于 Spring Boot 3.2 + Vue 3 + Element Plus 的资产管理系统

**技术栈**:
- **后端**: Spring Boot 3.2, MyBatis Plus, MySQL 8.0, Spring Security, JWT
- **前端**: Vue 3, Element Plus, Vite, Vue Router, Axios

---

## 已完成功能 ✅

### 1. 核心基础功能
- ✅ 用户登录认证 (JWT) - AuthController
- ✅ 工作台 Dashboard
- ✅ 通用响应封装 (Result, PageResult)
- ✅ 路由配置 (8 大模块，24 个子功能)

### 2. 基础信息模块
- ✅ 资产分类管理 (后端完整 CRUD + 树形查询)
  - Entity: AssetCategory
  - Mapper: AssetCategoryMapper
  - Service: AssetCategoryService + AssetCategoryServiceImpl
  - Controller: AssetCategoryController
  - 前端：CategoryList.vue ✅ (已完成：树形表格、搜索、新增/编辑/删除、子分类管理)
  
- ✅ 存放位置管理 (后端完整 CRUD + 树形查询)
  - Entity: AssetLocation
  - Mapper: AssetLocationMapper
  - Service: AssetLocationService + AssetLocationServiceImpl
  - Controller: AssetLocationController
  - 前端：LocationList.vue ✅ (已完成：树形表格、搜索、新增/编辑/删除、子位置管理)

- ✅ 部门管理 (后端完整 CRUD + 树形查询)
  - Entity: SysDepartment
  - Mapper: SysDepartmentMapper
  - Service: SysDepartmentService
  - Controller: SysDepartmentController
  - 前端：DepartmentList.vue ✅ (已完成：树形表格、搜索、新增/编辑/删除、子部门管理)
  - 已接入：采购申请、资产调拨部门选择器

### 3. 资产取得模块 (Acquisition)
- ✅ 采购申请 (完整 CRUD + 审批流程)
  - Entity: PurchaseRequest
  - Repository: PurchaseRequestRepository
  - Service: PurchaseRequestService + PurchaseRequestServiceImpl
  - Controller: PurchaseRequestController
  - 前端：PurchaseList.vue
  
- ✅ 验收登记 (完整 CRUD + 验收流程)
  - Entity: AssetAcceptance
  - Repository: AssetAcceptanceRepository
  - Service: AssetAcceptanceService + AssetAcceptanceServiceImpl
  - Controller: AssetAcceptanceController
  - 前端：AcceptanceList.vue

- ✅ 资产入库 (完整 CRUD + 确认入库)
  - Entity: AssetStorage
  - Repository: AssetStorageRepository
  - Service: AssetStorageService + AssetStorageServiceImpl
  - Controller: AssetStorageController
  - 前端：StorageList.vue

### 4. 资产管理模块 (Management)
- ✅ 领用退库 (完整 CRUD + 审批流程)
  - Entity: AssetRequisition
  - Repository: AssetRequisitionRepository
  - Service: AssetRequisitionService + AssetRequisitionServiceImpl
  - Controller: AssetRequisitionController
  - 前端：RequisitionList.vue

- ✅ 资产调拨 (完整 CRUD + 审批流程)
  - Entity: AssetTransfer
  - Mapper: AssetTransferMapper
  - Service: AssetTransferService + AssetTransferServiceImpl
  - Controller: AssetTransferController
  - 前端：TransferList.vue, TransferForm.vue, TransferDetail.vue (位于 transfer 目录)

- ✅ 资产维修 (完整 CRUD + 维修流程)
  - Entity: AssetMaintenance
  - Mapper: AssetMaintenanceMapper
  - Service: AssetMaintenanceService + AssetMaintenanceServiceImpl
  - Controller: AssetMaintenanceController
  - 前端：MaintenanceList.vue ✅ (已完成：列表查询、新增/编辑申请、审批、开始维修、完成维修、删除)

### 5. 资产档案模块 (Archive)
- ✅ 资产卡片管理 (完整 CRUD)
  - Entity: AssetCard
  - Mapper: AssetCardMapper
  - Service: AssetService
  - Controller: AssetController
  - 前端：AssetList.vue, AssetForm.vue, AssetDetail.vue

### 6. 资产盘点模块 (Inventory)
- ✅ 盘点任务 (完整 CRUD + 任务执行)
  - Entity: InventoryTask
  - Mapper: InventoryTaskMapper
  - Service: InventoryTaskService + InventoryTaskServiceImpl
  - Controller: InventoryTaskController
  - 前端：TaskList.vue
  - 数据库：inventory_task 表已创建

### 7. 数据库表 (当前 schema 共 16 张表)
- ✅ sys_user - 系统用户表
- ✅ sys_department - 系统部门表
- ✅ asset_category - 资产分类表
- ✅ asset_location - 资产存放位置表
- ✅ asset_card - 资产卡片表
- ✅ purchase_request - 采购申请表
- ✅ asset_acceptance - 验收登记表
- ✅ asset_storage - 资产入库表
- ✅ asset_requisition - 资产领用退库表
- ✅ asset_transfer - 资产调拨表
- ✅ asset_maintenance - 资产维修记录表
- ✅ inventory_task - 盘点任务表 (表结构已创建)

---

## 待开发功能 📋

### 一、档案扩展模块 (Archive Extension)
- [x] **附件管理** - 资产相关附件上传下载 ✅ (前端完成，模拟 API)
  - 前端：AttachmentList.vue (已完整实现：附件列表、上传、下载、预览、删除、批量操作)
- [ ] **图片管理** - 资产图片管理
- [ ] **文档管理** - 资产相关文档管理
- [x] **变更记录** - 资产变更历史记录 ✅ (前端完成，模拟 API)
  - 前端：ChangeList.vue (已完整实现：变更记录列表、新增、详情、导出)

### 二、资产管理模块 (Management) - 待扩展
- [x] **资产借用/归还** - 借用管理 ✅ (前后端完成)
  - 前端：BorrowList.vue (已完整实现：借用列表查询、审批、归还操作)
  - 后端：AssetBorrowController/Service/Mapper/Entity 完整实现
    - 功能：CRUD + 审批/归还/取消/逾期提醒
    - API: /api/asset/borrow/* (8 个接口)
  - 数据库：asset_borrow 表已创建
- [ ] **资产变更** - 资产信息变更
  - 需创建完整流程
- [x] **资产调拨** - 部门间资产调拨 ✅ (前后端完成)
  - 前端：transfer/TransferList.vue, TransferForm.vue, TransferDetail.vue (已完整实现)
  - 注意：management/TransferList.vue 是占位符，应使用 transfer 目录下的实现

### 三、资产处置模块 (Disposal)
- [x] **报废申请/处置审批/出售捐赠** - 资产处置全流程 ✅ (前后端完成)
  - 前端：ScrapList.vue, ApprovalList.vue, SaleList.vue (已完整实现)
  - 后端：AssetDisposalController/Service/Mapper/Entity 完整实现
    - 功能：CRUD + 审批/执行/完成/取消
    - API: /api/asset/disposal/* (8 个接口)
  - 数据库：asset_disposal 表已创建
- [ ] **残值回收** - 处置收益管理

### 四、资产定位模块 (Location Tracking)
- [ ] **位置监控** - 资产实时位置查看
- [ ] **轨迹追踪** - 资产移动历史记录

### 五、资产盘点模块 (Inventory)
- [x] **盘点任务** - 盘点任务执行 ✅ (前后端完成)
  - 前端：TaskList.vue (已完整实现)
  - 后端：InventoryTaskController/Service/Mapper/Entity 完整实现
  - 数据库：inventory_task 表已创建
- [x] **盘点计划** - 盘点计划创建 ✅ (前后端完成)
  - 前端：PlanList.vue (已完整实现：列表查询、新增/编辑、启用/停用、执行计划)
  - 后端：InventoryPlanController/Service/Mapper/Entity 完整实现
    - 功能：CRUD + 启用/停用/执行计划
    - API: /api/inventory/plan/* (7 个接口)
  - 数据库：inventory_plan 表已创建
- [x] **盘点结果** - 盘盈盘亏处理 ✅ (前后端完成)
  - 前端：ResultList.vue (已完整实现：列表查询、提交/复核/处理、批量导入)
  - 后端：InventoryResultController/Service/Mapper/Entity 完整实现
    - 功能：CRUD + 提交/复核/处理 + 批量导入
    - API: /api/inventory/result/* (9 个接口)
  - 数据库：inventory_result 表已创建

### 六、资产门户模块 (Portal)
- [x] **我的资产** - 个人名下资产查看 ✅ (前后端完成)
  - 前端：MyAssets.vue (已完整实现：列表展示、状态标签、分页、详情跳转)
  - 后端：AssetService.getMyAssets, AssetController./api/assets/my/list
- [x] **部门资产** - 部门资产视图 ✅ (前后端完成)
  - 前端：DeptAssets.vue (已完整实现：列表展示、使用人字段、状态标签、分页、详情跳转)
  - 后端：AssetService.getDeptAssets, AssetController./api/assets/dept/list
- [ ] **资产公告** - 资产管理相关通知

### 七、资产报表模块 (Report)
- [ ] **资产台账** - 资产总览报表
- [ ] **折旧报表** - 资产折旧明细
  - 前端：DepreciationReport.vue (占位符，待开发)
- [ ] **统计报表** - 多维度统计分析
  - 前端：StatisticsReport.vue (占位符，待开发)
- [ ] **处置报表** - 资产处置统计
  - 前端：DisposalReport.vue (占位符，待开发)

---

## 优先级队列 (按开发顺序)

### 🔴 P0 - 核心功能 (当前迭代) ✅ 全部完成
- [x] **资产卡片管理** - 建立完整的资产档案管理 (CRUD)
  - 状态：已完成 ✅
  - 后端：AssetController, AssetService, AssetCardMapper, AssetCard Entity
  - 前端：AssetList.vue, AssetForm.vue, AssetDetail.vue
  
- [x] **资产调拨** - 部门间资产调拨
  - 状态：已完成 ✅
  - 后端：AssetTransferController, AssetTransferService, AssetTransferMapper, AssetTransfer Entity
  - 前端：TransferList.vue, TransferForm.vue, TransferDetail.vue (位于 transfer 目录)
  - 数据库：asset_transfer 表已创建
  
- [x] **资产维修** - 维护资产状态记录
  - 状态：已完成 ✅
  - 后端：AssetMaintenanceController, AssetMaintenanceService, AssetMaintenanceMapper, AssetMaintenance Entity
  - 前端：MaintenanceList.vue (完整实现：列表查询、新增/编辑申请、审批、开始维修、完成维修、删除)
  - 数据库：asset_maintenance 表已创建

### 🟡 P1 - 重要功能 (下一迭代) ✅ 全部完成
- [x] **资产盘点任务** - 盘点任务执行功能 ✅
  - 状态：已完成 ✅
  - 后端：InventoryTaskController, InventoryTaskService, InventoryTaskMapper, InventoryTask Entity
  - 前端：TaskList.vue 完整实现（新建/编辑/详情/执行）
  - 数据库：inventory_task 表已创建
- [x] **资产盘点计划/结果** - 盘点全流程 ✅
  - 状态：已完成 ✅
  - 后端：InventoryPlanController/Service, InventoryResultController/Service 完整实现
  - 前端：PlanList.vue, ResultList.vue (已完整实现)
  - 数据库：inventory_plan, inventory_result 表已创建
- [x] **资产处置** - 资产生命周期结束管理 ✅
  - 状态：前后端均已完成
  - 后端：AssetDisposalController/Service 完整实现 (报废/出售/捐赠)
  - 前端：ScrapList.vue, ApprovalList.vue, SaleList.vue (已完整实现)
  - 数据库：asset_disposal 表已创建
- [x] **资产借用/归还** - 日常借用管理 ✅
  - 状态：已完成 ✅
  - 后端：AssetBorrowController/Service 完整实现
  - 前端：BorrowList.vue (已完整实现)
  - 数据库：asset_borrow 表已创建
- [x] **采购申请完善** - 新建/编辑/详情功能 ✅
  - 状态：已完成 ✅ (2025-06-03)
  - 前端：PurchaseList.vue 完整实现（新建申请对话框、编辑、详情查看、表单提交）
  - API：purchase.js 已创建封装所有接口调用
- [x] **审批人信息自动填充** - 后端 TODO 修复 ✅
  - 状态：已完成 ✅ (2025-06-03)
  - 后端：UserContextUtil 工具类创建
  - 修复：AssetRequisitionServiceImpl, AssetTransferController, PurchaseRequestServiceImpl

### 🟢 P2 - 辅助功能 (后续迭代)
- [ ] **报表统计** - 数据分析决策支持
  - 资产台账、折旧报表、统计报表、处置报表
- [x] **资产门户** - 用户体验提升 ✅ (前后端完成)
  - 我的资产、部门资产 (已完成)
- [ ] **资产公告** - 系统通知功能
- [x] **附件管理** - 资产相关文件管理 ✅ (前端完成，模拟 API)
- [x] **变更记录** - 资产变更历史记录 ✅ (前端完成，模拟 API)
- [ ] **资产定位** - 高级追踪功能 (前后端均需开发)
- [ ] **资产变更** - 资产信息变更流程 (前后端均需开发)
- [ ] **残值回收** - 处置收益管理 (前后端均需开发)

---

## 开发规范

### 后端规范
- Entity: 实体类，对应数据库表 (@Data, @TableName)
- Repository/Mapper: 数据访问层，继承 BaseMapper
- Service: 业务逻辑层接口
- ServiceImpl: 业务逻辑实现
- Controller: RESTful API 控制器
- DTO: 数据传输对象 (Result, PageResult)

### 前端规范
- views/: 页面组件
  - acquisition/: 资产取得 (PurchaseList, AcceptanceList, StorageList)
  - management/: 资产管理 (RequisitionList, TransferList, BorrowList)
  - asset/: 资产档案 (AssetList, AssetForm, AssetDetail)
  - archive/: 档案扩展 (AttachmentList, ChangeList, MaintenanceList)
  - disposal/: 资产处置 (ScrapList, SaleList, ApprovalList)
  - inventory/: 资产盘点 (PlanList, TaskList, ResultList)
  - portal/: 资产门户 (MyAssets, DeptAssets)
  - report/: 资产报表 (DepreciationReport, StatisticsReport, DisposalReport)
  - basic/: 基础信息 (CategoryList, LocationList)
- components/: 通用组件
- api/: API 调用封装
- utils/: 工具函数
- router/: 路由配置
- store/: 状态管理

### 命名规范
- 数据库表：snake_case (如：asset_card)
- Java 类：PascalCase (如：AssetCard)
- 变量方法：camelCase (如：assetName)
- 前端组件：PascalCase (如：AssetList.vue)

---

## 下一步开发建议

基于 2026-06-06 最新状态，轻量级单体主流程已基本闭环，工程治理、部门主数据和构建基线均已落地。下一步建议按“先补主数据，再进入系统管理/RBAC”的主线推进。

### 🔴 P1 - 下一迭代建议优先

1. **供应商管理**
   - 新增供应商表、实体、Mapper、Service、Controller、API 封装、前端页面和菜单入口。
   - 覆盖供应商名称、编码、联系人、联系电话、地址、状态、备注等基础字段。
   - 接入采购申请、验收、入库、资产卡片中的供应商选择，替换部分文本输入。

2. **资产主数据管理**
   - 新增资产主数据表和页面，维护资产名称、规格型号、分类、品牌、单位、默认价格、保修期等模板信息。
   - 当前已接入采购申请表单；下一步接入验收登记、资产入库和资产卡片表单，让常用资产从文本录入升级为选择器。

3. **构建基线修复** ✅
   - 后端 `mvn -q -DskipTests compile` 已恢复通过。
   - 前端 `npm run build` 已恢复通过。

### 🟡 P2 - 主数据稳定后推进

4. **系统管理与权限**
   - 用户管理、角色管理和菜单权限基础版已完成：用户 CRUD、角色 CRUD、部门绑定、状态维护、密码重置、角色权限勾选、菜单/路由可见性控制均已具备最小闭环。
   - 下一步仍需按钮权限、部门数据权限、后端细粒度权限校验和操作审计；当前 `SysUser` 仍是单角色模型，需要继续演进为更完整的 RBAC。

5. **盘点闭环深化**
   - 盘点任务明细、扫码盘点、盘盈登记、盘亏处理、盘点报告。
   - 落地 `InventoryPlanServiceImpl` 中“根据计划生成盘点任务”的 TODO。

6. **通知与预警体系**
   - 落地 `AssetBorrowServiceImpl` 中借用逾期提醒 TODO。
   - 扩展保修、保养、库存、报废等资产预警。

### 🟢 P3 - 长期规划

7. **高级功能模块**
   - 资产定位模块、资产变更流程、残值回收管理、资产公告。

8. **工程与体验优化**
   - 自动化测试、CI/CD、Docker 化部署、操作审计、批量导入导出、移动端适配。

---

## 技术债务与优化建议

### 代码层面
1. **统一 API 调用规范**: 确保所有前端页面使用统一的错误处理和 loading 状态
2. **组件复用**: 提取通用表单组件、表格组件，减少重复代码
3. **类型安全**: 考虑引入 TypeScript 提升代码质量

### 功能层面
1. **权限控制细化**: 当前基于角色的权限控制可进一步细化到按钮级别
2. **数据导出功能**: 为所有列表页面添加 Excel 导出功能
3. **批量操作优化**: 完善批量导入、批量删除等操作的体验

### 性能层面
1. **数据库索引优化**: 针对常用查询条件添加索引
2. **前端懒加载**: 对大型列表实施虚拟滚动
3. **缓存策略**: 对基础数据 (如分类、位置) 实施前端缓存

---

## 当前进度 (2026-06-05 更新)

- **总功能点**: 30 个已路由 views 页面

- **已完成**: 30 个已路由页面的基础功能闭环

  - 核心基础：用户认证、Dashboard ✅ (2)

  - 基础信息：资产分类、存放位置、部门管理 (后端 + 前端完整实现) ✅ (3)

  - 资产取得：采购申请、验收登记、资产入库 ✅ (3)

  - 资产管理：领用退库、资产调拨、资产维修、资产借用 ✅ (4)

  - 资产档案：资产卡片 ✅ (1)

  - 资产盘点：盘点任务、盘点计划、盘点结果 ✅ (3)

  - 资产处置：报废申请/处置审批/出售捐赠 (后端 + 前端完整实现) ✅ (3)

  - 资产门户：我的资产、部门资产 ✅ (2) - 2025-06-03 新增

  - 档案扩展：附件管理、变更记录、维保记录 (前端完整实现) ✅ (3)

  - 资产报表：折旧报表、统计报表、处置报表 ✅ (3) - 2025-06-03 新增

  - 前端页面：CategoryList, LocationList, DepartmentList, PurchaseList, AcceptanceList, StorageList, RequisitionList, BorrowList, TransferList/Form/Detail, MaintenanceList, AssetList/Form/Detail, TaskList, PlanList, ResultList, ScrapList, ApprovalList, SaleList, AttachmentList, ChangeList, MyAssets, DeptAssets, DepreciationReport, StatisticsReport, DisposalReport ✅ (30)

- **待开发/待深化**: 按钮/数据权限、盘点报告/扫码盘点、通知预警等完整蓝图扩展能力

- **完成率**: 约 90%（按轻量级单体当前范围估算；按完整医院资产管理蓝图仍有扩展空间）



**代码统计**:

- 后端 Java 文件：86 个 (Controller: 17, Entity: 16)

- 前端 Vue views 页面：30 个

- 数据库表：16 张


## 前端页面状态明细

### ✅ 已完整实现的页面 (30 个)
| 模块 | 页面文件 | 状态 |
|------|---------|------|
| 基础信息 | CategoryList.vue | ✅ 树形表格、CRUD、子分类管理 |
| 基础信息 | LocationList.vue | ✅ 树形表格、CRUD、子位置管理 |
| 基础信息 | DepartmentList.vue | ✅ 树形表格、CRUD、子部门管理 |
| 资产取得 | PurchaseList.vue | ✅ 采购申请完整流程（新建/编辑/详情/审批） |
| 资产取得 | AcceptanceList.vue | ✅ 验收登记完整流程 |
| 资产取得 | StorageList.vue | ✅ 资产入库完整流程 |
| 资产管理 | RequisitionList.vue | ✅ 领用退库完整流程 |
| 资产管理 | transfer/TransferList.vue | ✅ 调拨列表 |
| 资产管理 | transfer/TransferForm.vue | ✅ 调拨申请表单 |
| 资产管理 | transfer/TransferDetail.vue | ✅ 调拨详情 |
| 资产管理 | archive/MaintenanceList.vue | ✅ 维修申请、审批、执行完整流程 |
| 资产管理 | management/BorrowList.vue | ✅ 借用归还列表、审批、归还操作 |
| 资产处置 | disposal/ScrapList.vue | ✅ 报废申请列表、审批、删除操作 |
| 资产处置 | disposal/ApprovalList.vue | ✅ 处置审批列表、审批通过/拒绝、执行处置、完成/取消、查看详情 |
| 资产处置 | disposal/SaleList.vue | ✅ 出售捐赠列表、审批通过/拒绝、执行处置、完成/取消、查看详情 |
| 资产档案 | asset/AssetList.vue | ✅ 资产卡片列表 |
| 资产档案 | asset/AssetForm.vue | ✅ 资产卡片表单 |
| 资产档案 | asset/AssetDetail.vue | ✅ 资产卡片详情 |
| 资产盘点 | inventory/TaskList.vue | ✅ 盘点任务完整流程（新建/编辑/详情/执行） |
| 资产盘点 | inventory/PlanList.vue | ✅ 盘点计划列表、新增/编辑、启用/停用、执行 |
| 资产盘点 | inventory/ResultList.vue | ✅ 盘点结果列表、提交/复核/处理、批量导入 |
| 资产门户 | portal/MyAssets.vue | ✅ 我的资产列表、状态标签、分页、详情跳转 |
| 资产门户 | portal/DeptAssets.vue | ✅ 部门资产列表、使用人字段、状态标签、分页、详情跳转 |
| 资产报表 | report/DepreciationReport.vue | ✅ 折旧报表列表、图表展示、数据导出 |
| 资产报表 | report/StatisticsReport.vue | ✅ 统计报表、多维度分析、图表展示、数据导出 |
| 资产报表 | report/DisposalReport.vue | ✅ 处置报表列表、图表展示、数据导出 |
| 核心基础 | Dashboard.vue | ✅ 工作台 |
| 核心基础 | Login.vue | ✅ 登录页 |
| 档案扩展 | archive/AttachmentList.vue | ✅ 附件列表、上传、下载、预览、删除、批量操作 (真实 API) |
| 档案扩展 | archive/ChangeList.vue | ✅ 变更记录列表、新增、详情、导出 (真实 API) |

## 最近更新

### 2025-06-03: 资产报表模块开发完成与 TODO 文档更新 - 项目 100% 完成
- **完成情况**: 
  - ✅ DepreciationReport.vue：折旧报表页面完整实现
    - 功能：折旧报表列表查询、日期筛选、分类筛选、图表展示（ECharts）、数据导出 Excel
    - API：调用 /api/reports/depreciation 接口
  - ✅ StatisticsReport.vue：统计报表页面完整实现
    - 功能：多维度统计分析（按状态、分类、部门）、图表展示（饼图、柱状图）、数据导出 Excel
    - API：调用 /api/reports/statistics 接口
  - ✅ DisposalReport.vue：处置报表页面完整实现
    - 功能：处置报表列表查询、日期筛选、处置类型筛选、图表展示、数据导出 Excel
    - API：调用 /api/reports/disposal 接口
  - ✅ ReportController.java：报表控制器完整实现（6 个接口）
    - GET /api/reports/statistics - 获取统计报表
    - GET /api/reports/disposal - 获取处置报表
    - GET /api/reports/depreciation - 获取折旧报表
    - GET /api/reports/export/statistics - 导出统计报表
    - GET /api/reports/export/disposal - 导出处置报表
    - GET /api/reports/export/depreciation - 导出折旧报表
  - ✅ ReportServiceImpl.java：报表服务实现类完整实现
    - getStatisticsReport: 统计分析（总数、状态分布、分类分布、部门分布、金额统计）
    - getDisposalReport: 处置统计（分页列表、统计数据、图表数据）
    - getDepreciationReport: 折旧统计（分页列表、统计数据、图表数据）
    - exportStatisticsReport: 导出统计报表为 Excel
    - exportDisposalReport: 导出处置报表为 Excel
    - exportDepreciationReport: 导出折旧报表为 Excel
  - ✅ report.js：新增报表相关 API 函数封装
- **当时状态**:
  - 后端 Java 文件：79 个（Controller: 16, Service: 16）
  - 前端 Vue 页面：31 个（31 个完整实现，0 个占位符）
  - 数据库表：15 张
  - 整体完成率：100% (31/31 核心页面)
- **剩余 TODO 项**（P3 低优先级优化项）:
  - AssetBorrowServiceImpl:217 - 消息通知系统集成（P3）
  - InventoryPlanServiceImpl:149 - 盘点计划自动生成任务（P3）

### 2025-06-03: P2 资产门户模块开发完成与 TODO 文档更新
- **完成情况**: 
  - ✅ MyAssets.vue：我的资产列表页面完整实现
    - 功能：资产列表展示、状态标签、分页、详情跳转
    - API：调用 /api/assets/my/list 接口
  - ✅ DeptAssets.vue：部门资产列表页面完整实现
    - 功能：部门资产列表展示、使用人字段、状态标签、分页、详情跳转
    - API：调用 /api/assets/dept/list 接口
  - ✅ asset.js：新增 getMyAssets 和 getDeptAssets API 函数
  - ✅ AssetService.java：新增 getMyAssets 和 getDeptAssets 方法
  - ✅ AssetController.java：新增 /api/assets/my/list 和 /api/assets/dept/list 接口
- **当时状态**:
  - 后端 Java 文件：79 个
  - 前端 Vue 页面：31 个（28 个完整实现，3 个占位符）
  - 数据库表：15 张
  - 整体完成率：约 90% (28/31 核心页面)
- **剩余 TODO 项**（非 P2 优先级）:
  - AssetBorrowServiceImpl:217 - 消息通知系统集成（P3）
  - InventoryPlanServiceImpl:149 - 盘点计划自动生成任务（P2）

### 2025-06-03: P1 开发任务完成与 TODO 文档更新
- **完成情况**: 
  - ✅ PurchaseList.vue：新建申请对话框、编辑申请、查看详情功能完整实现
  - ✅ TaskList.vue：新建任务对话框、编辑任务、查看详情功能完整实现
  - ✅ 后端 UserContextUtil 工具类创建，提供 getCurrentUsername() 方法
  - ✅ 修复 AssetRequisitionServiceImpl 审批人信息自动填充
  - ✅ 修复 AssetTransferController 审批人信息获取
  - ✅ 清理所有 P1 相关的 TODO 注释
- **当时状态**:
  - 后端 Java 文件：79 个（新增 UserContextUtil）
  - 前端 Vue 页面：31 个（26 个完整实现，5 个占位符）
  - 数据库表：15 张
  - 整体完成率：约 93% (26/31 核心页面)
- **剩余 TODO 项**（非 P1 优先级）:
  - AssetBorrowServiceImpl:217 - 消息通知系统集成（P3）
  - InventoryPlanServiceImpl:149 - 盘点计划自动生成任务（P2）

### 2025-06-01: 项目进度分析与 TODO 文档更新 (第二次)
- **检查结果**: 
  - 发现 2 个页面包含 TODO 注释，需要完善功能
  - 发现 5 个占位符页面待开发
  - management/TransferList.vue 占位符问题已修复 (已删除)
  - AttachmentList.vue 和 ChangeList.vue 已从模拟 API 迁移到真实 API
- **更新内容**:
  - 更新前端页面状态明细表，新增"部分功能待完善的页面"分类
  - 标记 PurchaseList.vue 和 TaskList.vue 为⚠️部分完成状态
  - 更新下一步开发建议，将 TODO 项完善列为 P1 优先级
  - 移除 management/TransferList.vue 从占位符列表中
  - 更新 AttachmentList 和 ChangeList 为使用真实 API

### 2025-06-01: 项目进度分析与 TODO 文档更新
- **项目整体进度**: 89% (24/27 核心模块完成)
- **代码统计**: 
  - 后端 Java 文件：78 个 (Controller: 15, Entity: 15, Service: 15, ServiceImpl: 15, Mapper: 15)
  - 前端 Vue 文件：30 个 (views 页面，已完整实现：24 个，占位符：6 个)
  - 数据库表：11 张
- **更新内容**:
  - 分析项目完成情况，发现 `management/TransferList.vue` 是占位符，但完整的调拨功能已在 `transfer/` 目录下实现
  - 确认档案扩展模块 (AttachmentList, ChangeList) 已完整实现 (模拟 API)
  - 更新 TODO.md 中当前进度部分 (完成率从 96% 修正为 89%)
  - 更新前端页面状态明细表 (已完整实现 24 个页面，待开发 6 个页面)
  - 新增"技术债务与优化建议"章节，涵盖代码、功能、性能三个层面
  - 更新下一步开发建议，明确优先级和具体实施要点
- **下一步开发建议**:
  1. **P1 优先级**：修复 `management/TransferList.vue` 占位符问题
  2. **P2 优先级**：资产门户模块 (MyAssets, DeptAssets) - 可复用现有 AssetCard 查询接口
  3. **P3 优先级**：资产报表模块 (折旧报表、统计报表、处置报表) - 需图表库支持

- 2025-05-29: 项目进度分析与文档更新
  - 前端：inventory/PlanList.vue 完整实现 (盘点计划列表查询、新增/编辑、启用/停用、执行计划)
  - 前端：inventory/ResultList.vue 完整实现 (盘点结果列表查询、提交/复核/处理、批量导入)
  - API：inventory.js 新增盘点计划和盘点结果相关接口 (getPlanList, getPlanById, createPlan, updatePlan, deletePlan, executePlan, enablePlan, disablePlan, getResultList, getResultById, createResult, updateResult, deleteResult, submitResult, reviewResult, handleResult, importResult)
  - 更新 TODO.md 前端页面状态表

- 2025-05-29: 完成资产借用归还、报废申请前端页面开发
  - 前端：management/BorrowList.vue 完整实现 (借用列表查询、审批、归还操作)
  - 前端：disposal/ScrapList.vue 完整实现 (处置列表查询、审批、删除操作)
  - 更新 TODO.md 前端页面状态表

- 2025-05-29: 完成资产盘点计划、资产处置、资产借用功能开发 (后端)
  - 新增后端服务实现：
    - InventoryPlanServiceImpl: 盘点计划 CRUD + 启用/停用/执行
    - InventoryResultServiceImpl: 盘点结果 CRUD + 提交/复核/处理 + 批量导入
    - AssetDisposalServiceImpl: 资产处置 CRUD + 审批/执行/完成/取消
    - AssetBorrowServiceImpl: 资产借用 CRUD + 审批/归还/取消/逾期提醒
  - 新增后端控制器：
    - InventoryPlanController: /api/inventory/plan/* (7 个接口)
    - InventoryResultController: /api/inventory/result/* (9 个接口)
    - AssetDisposalController: /api/asset/disposal/* (8 个接口)
    - AssetBorrowController: /api/asset/borrow/* (8 个接口)
  - 数据库表已存在：inventory_plan, inventory_result, asset_disposal, asset_borrow

- 2025-05-29: 更新项目进度分析
  - 确认前端完整实现页面共 16 个
  - 确认占位符页面共 13 个
  - 后端 Controller 共 15 个，Entity 共 15 个
  
- 2025-05-25: 完成维保记录管理功能开发 (前端)
  - 新增 API：basic.js 中增加维保记录相关接口 (getMaintenanceList, getMaintenanceById, createMaintenance, updateMaintenance, deleteMaintenance, approveMaintenance, startMaintenance, completeMaintenance, cancelMaintenance)
  - 前端：MaintenanceList.vue 完整实现
    - 列表查询：支持按资产编码、资产名称、维修结果、审批状态筛选
    - 新增/编辑维保申请：包含资产信息、维修类型、故障类型、故障描述等字段
    - 审批流程：支持通过/拒绝审批，填写审批意见
    - 维修执行：开始维修 (指定维修人)、完成维修 (填写维修方式、费用、结果)
    - 详情查看：完整的维保记录信息展示
    - 删除功能：带二次确认的删除操作
  - 后端：AssetMaintenanceController/Service/Mapper 已存在
  
- 2024-xx-xx: 完成资产盘点任务功能开发 (前后端)
  - 后端：InventoryTaskController, InventoryTaskService, InventoryTaskServiceImpl, InventoryTaskMapper, InventoryTask Entity
  - 前端：TaskList.vue 完整实现
  - 数据库：inventory_task 表已创建
  
- 2025-05-25: 完成资产分类和存放位置管理前端开发
  - 新增 API：basic.js (包含分类和位置的 CRUD 接口)
  - 前端：CategoryList.vue 完整实现 (树形表格、搜索、新增/编辑/删除、子分类管理)
  - 前端：LocationList.vue 完整实现 (树形表格、搜索、新增/编辑/删除、子位置管理)
  - 后端：AssetCategoryController/Service/Mapper 已存在
  - 后端：AssetLocationController/Service/Mapper 已存在
  
- 2024-xx-xx: 完成资产维修功能开发 (前后端)
  - 后端：Entity, Mapper, Service, Controller 完整实现
  - 前端：archive/MaintenanceList.vue 完整实现 (维修申请、审批、执行全流程)
  - 数据库：asset_maintenance 表已创建
  
- 2024-xx-xx: 完成资产调拨功能开发 (前后端)
  - 后端：Entity, Mapper, Service, Controller 完整实现
  - 前端：TransferList, TransferForm, TransferDetail 完整实现 (位于 transfer 目录)
  - 数据库：asset_transfer 表已添加到 schema.sql
  
- 2024-xx-xx: 完成资产卡片功能开发 (前后端)
  - 后端：AssetController, AssetService, AssetCardMapper 完整实现
  - 前端：AssetList, AssetForm, AssetDetail 完整实现
  
- 2024-xx-xx: 完成领用退库功能开发 (前后端)
- 2024-xx-xx: 完成资产入库功能开发 (前后端)
- 2024-xx-xx: 完成验收登记功能开发 (前后端)
- 2024-xx-xx: 完成采购申请功能开发 (前后端)
- 2024-xx-xx: 完成基础框架搭建
# 资产管理系统 - 开发进度清单

## 2026-06-06 分类/位置/部门权限继续收口

- 已将资产分类、存放位置、部门管理纳入与供应商、资产主数据一致的细粒度权限口径：后端接口由角色判断切换为基于权限项的 `hasAuthority` 校验。
- 已补充 `basic:category:create/edit/delete`、`basic:location:create/edit/delete`、`basic:department:create/edit/delete` 权限项，并通过权限展开保持历史 `basic:category`、`basic:location`、`basic:department` 菜单权限的向下兼容。
- 前端 `CategoryList.vue`、`LocationList.vue`、`DepartmentList.vue` 已按新权限项收口新增、编辑、子级新增、删除按钮；角色管理页权限勾选和角色种子数据同步补齐。
- 当前 req/test/ux 轻量级文档基线继续满足要求；RBAC 下一阶段剩余缺口进一步收敛为更多业务模块细粒度鉴权、部门数据权限和操作审计日志。

## 2026-06-06 资产卡片权限继续收口

- 已将资产卡片列表、详情、新增、编辑、删除以及“我的资产/部门资产”查询链路切换到基于权限项的后端鉴权；`AssetController` 不再依赖粗粒度角色判断。
- 已补充 `asset:delete` 权限项，并在前端默认角色权限映射、角色管理页权限勾选与默认角色种子数据中同步落地；资产卡片读写权限现改为显式配置，避免只读角色被动继承写权限。
- 前端 `AssetList.vue` 已按 `asset:create`、`asset:edit`、`asset:delete` 收口新增、编辑、删除按钮可见性。
- 当前 req/test/ux 轻量级文档基线继续满足要求；RBAC 下一阶段剩余缺口进一步收敛为部门数据权限和操作审计日志。
